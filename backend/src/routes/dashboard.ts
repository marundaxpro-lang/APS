import type { App } from '../index.js';

// Dashboard routes have schema mismatches and need to be redesigned
// Keeping this stub to prevent import errors
export function registerDashboardRoutes(app: App) {
  app.logger.info('Dashboard routes registered (placeholder)');
}
