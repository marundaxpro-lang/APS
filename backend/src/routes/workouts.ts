import type { App } from '../index.js';

// Workout routes have schema mismatches and need to be redesigned
// Keeping this stub to prevent import errors
export function registerWorkoutRoutes(app: App) {
  app.logger.info('Workout routes registered (placeholder)');
}
